import { StatsScreen } from '@/features/stats'

/** `/stats` route (§7.6 · §8 Phase 6). Reached from the home (ledger) screen header. */
export default function StatsRoute() {
  return <StatsScreen />
}
